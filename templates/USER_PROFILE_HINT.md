User prefers concise, direct answers with concrete file paths and commands.

When memory is relevant, use the external Markdown wiki at `{MEMORY_ROOT}` instead of relying only on Hermes built-in MEMORY.md.

Do not store secrets in memory. Ask before enabling scheduled automation if the user has not already approved it.

